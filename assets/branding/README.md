## HuluChat 图标资源

已生成以下文件：

- `icon.svg`：矢量源文件（建议优先使用/编辑）
- `icon.png`：512×512（运行时窗口图标）
- `icon.ico`：多尺寸（Windows 打包 exe 图标）
- `icon-256.png` / `icon-1024.png`：额外导出尺寸

重新生成栅格图标：

```bash
python3 scripts/generate_brand_assets.py
```

